#include <SDL.h>
#include <iostream>
#include "Game.h"

int main(int argc, char* argv[])
{
	srand(time(0));//randomises time
	Game game;//creates game object which is initalised in the constructor, going to menu
	return 0;
}